import React from "react";
import { createRoot } from "react-dom/client";
import MontaureoPlatform from "./MontaureoPlatform.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MontaureoPlatform />
  </React.StrictMode>
);
